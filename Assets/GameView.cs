using UnityEngine;
using Game;

public class GameView : MonoBehaviour
{
    private Game game;

    void Start()
    {
        this.game = new Game();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
