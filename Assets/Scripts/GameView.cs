using UnityEngine;

public class GameView : MonoBehaviour
{
    private GameModel game;

    void Start()
    {
        game = new GameModel();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
